-- =========================
--        LOAD WIND UI
-- =========================
local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TeleportService = game:GetService("TeleportService")
local TweenService = game:GetService("TweenService")

local LocalPlayer = Players.LocalPlayer

local Running = false
local CurrentTween

-- =========================
-- RESPAWN SOUND
-- =========================
LocalPlayer.CharacterAdded:Connect(function(newChar)
    local fakeSound = Instance.new("Sound")
    fakeSound.Name = "SystemAudio"
    fakeSound.Volume = 0
    fakeSound.Looped = true
    fakeSound.Parent = newChar
end)

local CorrectKey = "1669"
local SavedInput = ""
local Unlocked = false

-- =========================
--        CREATE WINDOW
-- =========================
local Window = WindUI:CreateWindow({
    Title = "TK1669 https://discord.gg/tWndbkvXeQ",
    Icon = "key",
    Author = "By เสี๋ยคิวจัดให้ XD",
    Folder = "MyHub",
    Size = UDim2.fromOffset(580, 400),
    Theme = "Dark"
})

-- =========================
--        KEY TAB
-- =========================
local KeyTab = Window:Tab({
    Title = "Key System",
    Icon = "key"
})

KeyTab:Section({ Title = "Enter Key" })

KeyTab:Input({
    Title = "ใส่ Key",
    Placeholder = "Enter key...",
    Callback = function(text)
        SavedInput = text
    end
})

-- =========================
-- SETTINGS TAB
-- =========================
local SettingsTab = Window:Tab({
    Title = "Discrod & instagram",
    Icon = "settings"
})

SettingsTab:Button({
    Title = "รีเข้าแมพ",
    Callback = function()
        TeleportService:Teleport(game.PlaceId)
    end
})

SettingsTab:Button({
    Title = "ปิด UI",
    Callback = function()
        local gui = LocalPlayer.PlayerGui:FindFirstChild("MyHub")
        if gui then gui:Destroy() end
    end
})

SettingsTab:Section({ Title = "Contact" })

SettingsTab:Button({
    Title = "คัดลอก IG 📸",
    Callback = function()
        if setclipboard then
            setclipboard("https://www.instagram.com/ezmxyxii/")
        end

        WindUI:Notify({
            Title = "สำเร็จ",
            Description = "คัดลอก IG แล้ว ✅",
            Duration = 2
        })
    end
})

SettingsTab:Button({
    Title = "คัดลอก Discord 💬",
    Callback = function()
        if setclipboard then
            setclipboard("https://discord.gg/XKJTssDUSR")
        end

        WindUI:Notify({
            Title = "สำเร็จ",
            Description = "คัดลอก Discord แล้ว ✅",
            Duration = 2
        })
    end
})

-- =========================
-- LOAD MAIN
-- =========================
local function LoadMain()

    local MainTab = Window:Tab({
        Title = "AutoFarm",
        Icon = "map-pin"
    })

    local Points = {
        Vector3.new(-60,120,1372),
        Vector3.new(-60,120,2137),
        Vector3.new(-60,120,2916),
        Vector3.new(-60,120,3681),
        Vector3.new(-60,120,4444),
        Vector3.new(-60,120,5214),
        Vector3.new(-60,120,5973),
        Vector3.new(-60,120,6745),
        Vector3.new(-60,120,7529),
        Vector3.new(-60,120,8302),
        Vector3.new(-60,-354,9492)
    }

    local SPEED = 400
    local WAIT_TIME = 16

    local function ApplyFloat(hrp)
        local old = hrp:FindFirstChild("AutoFloat")
        if old then old:Destroy() end

        local float = Instance.new("BodyVelocity")
        float.Name = "AutoFloat"
        float.MaxForce = Vector3.new(0, math.huge, 0)
        float.Parent = hrp
    end

    local function RemoveFloat(hrp)
        local old = hrp:FindFirstChild("AutoFloat")
        if old then old:Destroy() end
    end

    local function SmoothMove(hrp, pos)
        if CurrentTween then
            CurrentTween:Cancel()
        end

        local distance = (hrp.Position - pos).Magnitude
        local time = distance / SPEED

        CurrentTween = TweenService:Create(
            hrp,
            TweenInfo.new(time, Enum.EasingStyle.Sine, Enum.EasingDirection.InOut),
            {CFrame = CFrame.new(pos)}
        )

        CurrentTween:Play()
        CurrentTween.Completed:Wait()

        return Running
    end

    local tick = Instance.new("Sound", LocalPlayer.PlayerGui)
    tick.SoundId = "rbxassetid://9113420770"
    tick.Volume = 1

    local final = Instance.new("Sound", LocalPlayer.PlayerGui)
    final.SoundId = "rbxassetid://6026984224"
    final.Volume = 1

    local function AutoTeleportLoop()
        while Running do

            local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
            local hrp = char:WaitForChild("HumanoidRootPart")

            ApplyFloat(hrp)

            for i, pos in ipairs(Points) do
                if not Running then return end
                if not SmoothMove(hrp, pos) then return end
                task.wait(0.2)

                if i == #Points then
                    RemoveFloat(hrp)

                    for t = WAIT_TIME, 1, -1 do
                        if not Running then break end

                        if t <= 3 then
                            final:Play()
                        else
                            tick:Play()
                        end

                        task.wait(1)
                    end
                end
            end
        end
    end

    MainTab:Toggle({
        Title = "เปิด / ปิด Auto Farm",
        Callback = function(state)
            Running = state

            if state then
                WindUI:Notify({
                    Title = "Auto TP",
                    Description = "ระบบเริ่มทำงานแล้ว",
                    Duration = 2
                })
                task.spawn(AutoTeleportLoop)
            else
                if CurrentTween then
                    CurrentTween:Cancel()
                end

                WindUI:Notify({
                    Title = "Auto TP",
                    Description = "ระบบหยุดทำงาน",
                    Duration = 2
                })
            end
        end
    })
end

-- =========================
-- KEY BUTTON
-- =========================
KeyTab:Button({
    Title = "ยืนยันคีย์",
    Callback = function()
        if SavedInput == CorrectKey and not Unlocked then
            Unlocked = true

            WindUI:Notify({
                Title = "สำเร็จ",
                Description = "Key ถูกต้อง ✅",
                Duration = 2
            })

            LoadMain()
        else
            WindUI:Notify({
                Title = "ผิดพลาด",
                Description = "Key ไม่ถูกต้อง ❌",
                Duration = 3
            })
        end
    end
})
